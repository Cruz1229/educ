import React from 'react';
import { createRoot } from 'react-dom/client';
import CromixMobile from './CromixMobile';

createRoot(document.getElementById('root')).render(
  <React.StrictMode><CromixMobile /></React.StrictMode>,
);
